---
import { SITE_CONTENT } from "@config";
import ProjectLayout from "@layouts/ProjectLayout.astro";
import ProjectCarousel from "@components/ProjectCarousel.astro";
import type { ProjectProps } from "@types";

export function getStaticPaths() {
  return SITE_CONTENT.projects
    .filter((p) => Boolean(p.slug))
    .map((p) => ({ params: { slug: p.slug }, props: { project: p } }));
}

interface PageProps {
  project: ProjectProps;
}

const { project } = Astro.props as PageProps;

const {
  name,
  summary,
  longSummary,
  challenges,
  results,
  role,
  image,
  screenshots = [],
  implementationDetails,
  linkPreview,
  linkGooglePlay,
  linkAppStore,
  tech = [],
} = project;
---

<ProjectLayout>
  <article class="space-y-12">
    <!-- Project Header -->
    <header class="relative">
      <!-- Background with subtle pattern -->
      <div
        class="absolute inset-0 rounded-3xl bg-gradient-to-br from-gray-900/50 to-black/50"
      >
      </div>

      <div
        class="relative overflow-hidden rounded-3xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800"
      >
        <!-- Back Navigation -->
        <div class="border-b border-gray-700 p-6">
          <a
            href="/#projects"
            class="inline-flex items-center text-sm text-gray-400 transition-colors hover:text-white"
          >
            <svg
              class="mr-2 h-4 w-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M15 19l-7-7 7-7"></path>
            </svg>
            Back to Projects
          </a>
        </div>

        <div class="grid gap-8 p-8 lg:grid-cols-2">
          <!-- Project Info -->
          <div class="space-y-6">
            <div>
              <h1
                class="mb-4 font-serif text-4xl leading-tight font-bold text-white md:text-5xl"
              >
                {name}
              </h1>
              <p class="text-lg leading-relaxed text-gray-300">
                {summary}
              </p>
            </div>

            <!-- Action Buttons -->
            <div class="flex flex-wrap gap-4 pt-4">
              {
                linkPreview && (
                  <a
                    href={linkPreview}
                    target="_blank"
                    class="inline-flex items-center gap-2 rounded-lg bg-gradient-to-r from-pink-500 to-purple-600 px-6 py-3 font-medium text-white transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-pink-500/25"
                  >
                    <svg
                      class="h-4 w-4"
                      fill="none"
                      stroke="currentColor"
                      viewBox="0 0 24 24"
                    >
                      <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
                      />
                    </svg>
                    Live Preview
                  </a>
                )
              }
              {
                linkGooglePlay && (
                    <a
                      href={linkGooglePlay}
                      target="_blank"
                      class="inline-flex items-center gap-2 rounded-lg bg-gray-700 px-6 py-3 font-medium text-white transition-all duration-300 hover:scale-105 hover:bg-gray-600"
                    >
                      <svg
                        class="h-4 w-4"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"
                        />
                      </svg>
                      Google Play
                    </a>
                  )
              }
               {
                linkAppStore && (
                    <a
                      href={linkAppStore}
                      target="_blank"
                      class="inline-flex items-center gap-2 rounded-lg bg-gray-700 px-6 py-3 font-medium text-white transition-all duration-300 hover:scale-105 hover:bg-gray-600"
                    >
                      <svg
                        class="h-4 w-4"
                        fill="none"
                        stroke="currentColor"
                        viewBox="0 0 24 24"
                      >
                        <path
                          stroke-linecap="round"
                          stroke-linejoin="round"
                          stroke-width="2"
                          d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"
                        />
                      </svg>
                      App Store
                    </a>
                  )
              }
            </div>
          </div>

          <!-- Project Image -->
          <div class="relative">
            <div
              class="relative overflow-hidden rounded-2xl border border-gray-600 shadow-2xl"
            >
              <img
                src={image}
                width="800"
                height="500"
                alt={name}
                class="h-full w-full object-cover"
              />
              <div
                class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"
              >
              </div>
            </div>
          </div>
        </div>
      </div>
    </header>

    <!-- Project Details -->
    {/* Project Screenshots Carousel */}
    {
      screenshots.length > 0 && (
        <section class="space-y-8">
          <h2 class="text-2xl font-semibold text-white">Screenshots</h2>
          <ProjectCarousel screenshots={screenshots} projectName={name} />
        </section>
      )
    }

    <section class="grid gap-8 lg:grid-cols-2">
      <div class="space-y-8">
        <div
          class="rounded-2xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 p-8"
        >
          <h2 class="mb-6 text-2xl font-semibold text-white">
            Project Overview
          </h2>
          {
            longSummary && (
              <div class="prose prose-invert max-w-none">
                <div class="whitespace-pre-line">{longSummary}</div>
              </div>
            )
          }
        </div>

      </div>

      <div class="space-y-8">
        {
          role && (
            <div class="rounded-2xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 p-8">
              <h3 class="mb-2 text-sm font-medium text-gray-400">My Role</h3>
              <p class="text-lg font-medium text-white">{role}</p>
            </div>
          )
        }

        {
          tech && tech.length > 0 && (
            <div class="rounded-2xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 p-8">
              <h3 class="mb-4 text-lg font-medium text-white">
                Technologies Used
              </h3>
              <div class="flex flex-wrap gap-2">
                {tech.map((item) => (
                  <span class="rounded-full bg-gray-700 px-3 py-1 text-sm text-gray-200">
                    {item}
                  </span>
                ))}
              </div>
            </div>
          )
        }

        {
          challenges && (
            <div class="rounded-2xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 p-8">
              <h3 class="mb-3 text-lg font-medium text-white">Challenges</h3>
              <div class="prose prose-invert max-w-none text-gray-300">
                <p>{challenges}</p>
              </div>
            </div>
          )
        }

        {
          results && (
            <div class="rounded-2xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 p-8">
              <h3 class="mb-3 text-lg font-medium text-white">Results</h3>
              <div class="prose prose-invert max-w-none text-gray-300">
                <p>{results}</p>
              </div>
            </div>
          )
        }
      </div>
    </section>
  </article>

  <!-- Project Showcase Gallery -->
  <section class="space-y-8 py-20">
    <div class="text-center">
      <h2 class="mb-4 font-serif text-3xl font-bold text-white md:text-4xl">
        Project Showcase
      </h2>
      <p class="mx-auto max-w-2xl text-xl text-gray-400">
        Visual highlights and key features of the project
      </p>
    </div>

    {
      (() => {
        const items = SITE_CONTENT.projects
          .filter((p) => p.slug && p.slug !== project.slug)
          .slice(0, 6);
        return (
          <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
            {items.map((p) => (
              <article class="group relative overflow-hidden rounded-2xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 transition-all duration-300 hover:border-gray-600 hover:shadow-2xl hover:shadow-purple-500/10">
                <a
                  href={`/projects/${p.slug}`}
                  aria-label={`${p.name} details`}
                >
                  <div class="relative aspect-video overflow-hidden">
                    <img
                      src={p.image}
                      alt={p.name}
                      width="600"
                      height="350"
                      class="h-full w-full object-cover object-center transition-transform duration-500 group-hover:scale-110"
                    />
                    <div class="absolute inset-0 bg-gradient-to-t from-black/60 via-transparent to-transparent" />
                  </div>
                </a>
                <div class="p-6">
                  <h3 class="mb-2 text-lg font-semibold text-white transition-colors duration-300 group-hover:text-purple-300">
                    <a href={`/projects/${p.slug}`} class="hover:underline">
                      {p.name}
                    </a>
                  </h3>
                  <p class="line-clamp-2 text-sm leading-relaxed text-gray-300">
                    {p.summary}
                  </p>
                </div>

                <div class="pointer-events-none absolute inset-0 bg-gradient-to-t from-purple-900/20 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100" />
              </article>
            ))}
          </div>
        );
      })()
    }
  </section>

  <!-- Call to Action -->
  <section
    class="mt-16 rounded-3xl border border-gray-700 bg-gradient-to-br from-gray-900 to-gray-800 p-12 text-center"
  >
    <div class="mx-auto max-w-2xl space-y-6">
      <h3 class="text-3xl font-bold text-white">
        Interested in {name}?
      </h3>
      <p class="text-lg leading-relaxed text-gray-300">
        Want to learn more about this project or discuss similar work? I'd love
        to share more details about the development process and technical
        decisions.
      </p>
      <div class="flex flex-wrap justify-center gap-4 pt-4">
        <a
          href="/#projects"
          class="inline-flex items-center gap-2 rounded-full bg-gradient-to-r from-purple-600 to-pink-600 px-8 py-4 font-medium text-white transition-all duration-300 hover:scale-105 hover:from-purple-700 hover:to-pink-700 hover:shadow-lg hover:shadow-purple-500/25"
        >
          <svg
            class="h-4 w-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M15 19l-7-7 7-7"></path>
          </svg>
          Back to Projects
        </a>
        <a
          href="mailto:campos.esteban@gmail.com"
          class="inline-flex items-center gap-2 rounded-full bg-gray-700 px-8 py-4 font-medium text-white transition-all duration-300 hover:scale-105 hover:bg-gray-600"
        >
          <svg
            class="h-4 w-4"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
            ></path>
          </svg>
          Get in Touch
        </a>
      </div>
    </div>
  </section>
</ProjectLayout>
